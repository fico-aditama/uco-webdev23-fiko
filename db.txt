  git init
  git add README.md
  git commit -m "first commit"
  git branch -M main
  git remote add origin git@github.com:fico-aditama/uco-webdev23-fiko.git
  git push -u origin main


php artisan make:controller HomeController
php artisan make:controller ArticleController

# sesi 3
# https://elearn.uc.ac.id/mod/assign/view.php?id=340692
# Assuming you've already initialized a Git repository and committed your changes before this point.

# Step 1: Create a new branch "sesi-3"
git checkout -b sesi-3

# Step 2: Add and commit your changes to the new branch
git add .
git commit -m "sesi-3 changes"

# Step 3: Add a remote repository (if not added already)
# Note: You might have already added the remote repository in a previous step.
git remote add origin https://github.com/cameronmcnz/example-website.git

# Step 4: Push changes to the "sesi-3" branch on the remote repository
# Use -u only for the first push to set up tracking
git push -u origin sesi-3
